var Aloha = window.Aloha || ( window.Aloha = {} );

Aloha.settings = {
  jQuery: $,
  baseUrl: '/assets/aloha/',
  plugins: {
    load: "common/format, common/table, common/list, common/link, common/paste, common/image, common/undo"
  }
};
